class TokenStoreService {
    static async storeToken (key, token) {
        // await set(key, token);
    }

    static async getToken (key) {
        // await get(key);
    }
}

module.exports = TokenStoreService;